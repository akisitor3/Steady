{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "content=$(cat \"/Volumes/KHONSHU/Brain/Steady/LAST_SESSION.md\" 2>/dev/null | head -80) && printf \"%s\" \"$content\" | python3 -c \"import sys,json; print(json.dumps({'hookSpecificOutput':{'hookEventName':'SessionStart','additionalContext':'=== STEADY — ÚLTIMA SESSÃO ===\\n' + sys.stdin.read()}}))\"",
            "statusMessage": "A carregar contexto do Steady..."
          }
        ]
      }
    ]
  }
}
